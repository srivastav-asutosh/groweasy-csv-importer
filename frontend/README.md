# GrowEasy CSV Importer — Frontend

Next.js frontend for the GrowEasy AI CSV importer. See the [project README](../README.md) for the full architecture, setup instructions, and API docs.

Quick start:

```bash
cp .env.local.example .env.local
npm install
npm run dev
```
